# PRD: 포켓몬 도감 (스칼렛/바이올렛 프로토타입)

## Problem Statement
포켓몬을 5년 이상 플레이해 온 사용자가, 포켓몬의 타입·진화 정보·기술·입수 방법이 궁금할 때마다 매번 인터넷을 검색하는 번거로움을 겪고 있음. 검색할 때마다 여러 사이트를 오가며 정보를 취합해야 해서 시간이 소요되고, 오프라인이거나 인터넷이 느릴 때는 이용이 불편함. 자주 찾는 정보를 로컬에 미리 정리해두면 즉시 조회할 수 있어 이 반복적인 불편을 없앨 수 있음.

## Goals
1. 스칼렛/바이올렛(파르데아 도감) 포켓몬을 한글 이름으로 검색하면 1초 이내(체감상 즉시)로 결과가 표시된다.
2. 인터넷 재검색 없이 타입·진화 정보·배우는 기술·입수 방법을 한 화면에서 확인할 수 있다.
3. 별도 서버/설치 없이 브라우저(HTML/CSS/JS)만으로 로컬에서 실행된다.
4. 오타나 일부만 입력해도(자동완성) 원하는 포켓몬을 빠르게 찾을 수 있다.

## Non-Goals
- **스칼렛/바이올렛 외 다른 세대·시리즈 지원** — 이번 프로토타입 범위 밖. 추후 확장 여지만 남겨둠.
- **다중 사용자/로그인/즐겨찾기 동기화** — 개인용 단일 사용자 도구이므로 불필요.
- **실시간 API 연동(PokeAPI 등)** — 딜레이 최소화가 목표라 사전 크롤링한 로컬 데이터 사용이 우선.
- **종족값(스탯)·특성(어빌리티) 정보** — 이번 요구사항에 포함되지 않음. 필요해지면 P2로 재검토.
- **모바일 앱/설치형 프로그램** — 브라우저 기반으로 충분.

## User Stories
- 사용자로서, 나는 포켓몬 한글 이름을 입력하면 해당 포켓몬 정보를 보고 싶다 — 매번 인터넷을 검색하지 않기 위해.
- 사용자로서, 나는 이름을 일부만 입력해도 자동완성으로 후보가 뜨길 원한다 — 정확한 철자를 몰라도 빠르게 찾기 위해.
- 사용자로서, 나는 포켓몬의 타입을 보고 싶다 — 상성을 바로 파악하기 위해.
- 사용자로서, 나는 진화 전/후 포켓몬과 진화 조건(레벨, 아이템, 친밀도 등)을 보고 싶다 — 진화를 놓치지 않기 위해.
- 사용자로서, 나는 스칼렛/바이올렛에서 이 포켓몬을 어디서 잡을 수 있는지 보고 싶다 — 사냥을 나가기 전에 위치를 알기 위해.
- 사용자로서, 나는 이 포켓몬이 배우는 기술 목록을 보고 싶다 — 파티 구성을 짜기 위해.
- 사용자로서, 나는 존재하지 않는 이름을 입력했을 때 "결과 없음" 같은 명확한 안내를 보고 싶다 — 오타인지 헷갈리지 않기 위해.

## Requirements

### Must-Have (P0)
1. **한글 이름 검색 + 자동완성(부분일치)**
   - Acceptance Criteria:
     - [ ] 검색창에 두 글자 이상 입력하면 일치하는 포켓몬 이름 후보가 드롭다운으로 표시된다
     - [ ] 후보 클릭 또는 Enter 시 해당 포켓몬 상세 정보가 표시된다
     - [ ] 존재하지 않는 이름 입력 시 "검색 결과 없음" 메시지가 표시된다
2. **포켓몬 상세 정보 표시**: 타입, 진화 정보, 배우는 기술, 스칼렛/바이올렛 입수 방법
   - Acceptance Criteria:
     - [ ] 타입이 1~2개 아이콘/텍스트로 표시된다
     - [ ] 진화 체인(이전/다음 단계)과 진화 조건이 표시된다 (진화하지 않는 포켓몬은 "진화 없음" 표시)
     - [ ] 레벨업으로 배우는 기술이 레벨 순으로 목록 표시된다
     - [ ] 스칼렛/바이올렛 내 입수 방법(필드 조우 지역, 특전, 알, 교환 등)이 표시된다
3. **로컬 데이터 기반 즉시 조회**
   - Acceptance Criteria:
     - [ ] 사전 크롤링된 데이터가 CSV 또는 JSON/로컬 DB 형태로 프로젝트에 포함되어 있다
     - [ ] 페이지 로드 시 데이터를 한 번만 불러오고, 이후 검색은 네트워크 요청 없이 메모리에서 처리된다
     - [ ] 검색 결과 표시까지 체감 딜레이가 거의 없다(로컬 처리 기준 100ms 내외)
4. **초기 데이터 수집 스크립트**
   - Acceptance Criteria:
     - [ ] 포켓몬 위키(Fandom 한국어판)에서 파르데아 도감 포켓몬 전체(DLC "남청의 원반"·"짙은보랏빛의 가면" 추가분 포함, 정확한 수는 크롤링 시 확정)의 정보를 크롤링하는 스크립트가 있다
     - [ ] 크롤링 결과가 CSV 또는 JSON 파일로 로컬에 저장된다
     - [ ] 요청 간 딜레이를 둬서 대상 사이트에 과도한 부하를 주지 않는다
5. **기술머신(TM) 기술 표시**
   - Acceptance Criteria:
     - [ ] 레벨업 기술 목록과 구분되는 별도 섹션으로 해당 포켓몬이 배울 수 있는 기술머신 기술 목록이 표시된다
     - [ ] 기술머신 번호(TM 번호) 또는 기술 이름 기준으로 정렬되어 표시된다

### Nice-to-Have (P1)
- 포켓몬 이미지(공식 아트/도트) 표시
- 진화 체인을 클릭하면 해당 진화체 정보로 바로 이동
- 최근 검색한 포켓몬 목록(로컬에서만, `localStorage` 활용)
- 타입 상성(약점/저항) 자동 계산 표시

### Future Considerations (P2)
- 스칼렛/바이올렛 외 다른 세대/시리즈로 확장 (시리즈별 입수 방법 비교)
- 종족값(스탯), 특성(어빌리티) 정보 추가
- 알 기술(Egg Move)까지 포함한 전체 기술 목록
- 다크모드, 반응형(모바일 브라우저) 대응

## Success Metrics
**Leading (즉시 확인 가능)**
- 파르데아 도감 전체 포켓몬 데이터 수집 완료율: 100%
- 검색 → 결과 표시까지 체감 딜레이: 100ms 이내 (로컬 데이터 기준)
- 임의로 뽑은 10마리 포켓몬 정보를 실제 위키와 대조했을 때 정확도: 95% 이상

**Lagging (사용하면서 확인)**
- "인터넷 검색보다 이 도구가 더 빠르고 편한가?" — 본인이 실제로 인터넷 검색 대신 이 도구를 계속 쓰는지 여부로 판단

## Open Questions
- **[엔지니어링]** 포켓몬 위키 페이지 구조가 포켓몬마다 100% 동일하지 않을 수 있어, 크롤링 스크립트가 예외 케이스(폼 체인지 포켓몬 등)를 어떻게 처리할지 확인 필요.

> DLC 포함 여부, 기술머신 표시 여부는 확정됨 (2026-09-02) — 아래 반영됨.

## Timeline Considerations
- 하드 데드라인 없음(개인 프로젝트).
- 단계 제안: ① 크롤링 스크립트로 데이터 수집·검증 → ② 로컬 데이터 포맷 확정(CSV vs JSON) → ③ 검색/자동완성 UI → ④ 상세 정보 표시 UI.

## 데이터 출처
- [포켓몬 위키(Fandom) 한국어판](https://pokemon.fandom.com/ko/) — 페이지 구조가 일관되어 크롤링 자동화 용이, 한글 정보 제공.
- 개인 학습/개인용 도구 목적. `robots.txt` 확인, 요청 간 딜레이, 1회성 수집(재배포 없이 로컬 저장용) 원칙 준수.
